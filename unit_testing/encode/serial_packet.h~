#ifndef __SERIAL_PACKET_H
#define __SERIAL_PACKET_H

/* 
 * @brief
 * serial packet tx buffer size\n
 * must be bigger than single packet size, else packet always corrupted
 * @attention set first thing
 */
#define SP_TX_BUF_SIZE 
void serial_encode(uint8_t payload_id, uint8_t payload_size, void *payload_ptr);

#endif	/* __SERIAL_PACKET_H */
